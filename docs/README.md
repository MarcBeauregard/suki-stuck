# Suki Stuck — $SUKISTUCK

**Launch-ready site · Solana · CA TBA · NO MINT from agents**

Fan meme craft. Not affiliated with Ventura County Fire Department or the kitten’s owner. No intrinsic value.

## Thesis

The kitten who got stuck in the entertainment center — **the cabinet hole is the brand.**

## Character lock

| Asset | Role |
|-------|------|
| `assets/suki-hero.png?v=4` | Hero scene + Open Graph |
| `assets/suki-icon.png?v=4` | Circular header / avatar (centered crop) |
| `assets/suki-logo.png?v=4` | Logo fallback |
| `assets/suki-master.png?v=4` | Fallback / source of truth |

Habitat = brand (SHRUB-class). Wood-night palette. No white plate. No Connect Wallet. No fake metrics. No invented CA.

## Sections

1. **Hero** — stuck-in-cabinet scene (`suki-hero.png`) + circular avatar (`suki-icon.png`)
2. **Why stuck** — habitat / occlusion / rescue energy
3. **Hole doctrine** — nine-lives theater + species card
4. **Token** — Name Suki Stuck · Ticker `$SUKISTUCK` · Network Solana · CA `TBA` (copy enabled → toast)
5. **Buy on Solana** — wallet → Block X / Raydium / Jupiter when CA live → paste CA
6. **Trust** — not VCFD affiliated · no intrinsic value
7. **Footer** — RD / LA Times / NY Post / Newser + disclaimer · subtle CA TBA

## Local preview

```bash
cd sites/builds/suki-cabinet-kitten
python3 -m http.server 8766
```

→ http://127.0.0.1:8766/

## Pages pack

`docs/` mirrors root HTML/CSS/JS/assets for GitHub Pages (`main` → `/docs` + `.nojekyll`).

Repo: https://github.com/MarcBeauregard/suki-stuck  
Pages: https://marcbeauregard.github.io/suki-stuck/

## Status

Launch-ready UI. Contract = **TBA** until Marc mints. Agents do **not** mint. `robots` stays `noindex` until Marc says otherwise.
